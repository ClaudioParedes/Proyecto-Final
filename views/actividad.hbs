<div class="row container px-sm-5 py-5 justify-content-center mx-auto">
 <div class="my-3"><h1>Datos Sector y Actividad</h1></div>
 <hr />
<div class="border shadow col-12 col-md-10 col-lg-9">
       
      <div class="container">
          <div class="row align-items-center my-5">
              <div class="col-12 col-md-6">
                  <h5>NIVEL</h5>
                      <select id="nivel" class="form-select" aria-label="Default select example">
                    
                     {{#each niveles}}
                        <option value="{{this.id_nivel}}">{{this.nombre}}</option>
                     {{/each}}
            </select>
                
              </div>

              <div class="col-12 col-md-6">
                  <h5>ÁREA</h5>
            <select id="area" class="form-select" aria-label="Default select example">
                {{#each area}}
                        <option value="{{this.id_area}}">{{this.nombre}}</option>
                {{/each}}
            </select>   

              </div>
          </div>
      </div>
      <div class="container">
          <div class="row align-items-center my-5">
              <div class="col-12 col-md-6">
                  <h5>ACTIVIDAD</h5>
             <select id="actividad" class="form-select" aria-label="Default select example">
                {{#each actividad}}
                        <option value="{{this.id_actividad}}">{{this.nombre}}</option>
                     {{/each}}
            </select>
              </div>

              <div class="col-12 col-md-6">
                  <h5>CONTRATO</h5>
            <select id="gerencia" class="form-select" aria-label="Default select example">
               
                {{#each contrato}}
                        <option value="{{this.id_contrato}}">{{this.nro_contrato}}</option>
                     {{/each}}
            </select>
              </div>
          </div>
     </div>
<div class="container">
          <div class="row align-items-right my-5">
              <div class="col-12 col-md-6">
                <button id="anterior" class="btn btn-primary">Anterior</button>
                <button id="siguiente" class="btn btn-primary">Siguiente</button>
              </div>
         </div>
</div>
</div>
</div>

<script>
    const siguiente = document.getElementById("siguiente")
    siguiente.addEventListener("click", async (e) => {
      const nivelJ = document.getElementById("nivel")
      const areaJ = document.getElementById("area")
      const actividadJ = document.getElementById("actividad")
      const gerenciaJ = document.getElementById("gerencia")
      const nivelJSON = nivelJ.options[nivelJ.selectedIndex].text;
      const areaJSON = areaJ.options[areaJ.selectedIndex].text;
      const actividadJSON = actividadJ.options[actividadJ.selectedIndex].text;
      const gerenciaJSON = gerenciaJ.options[gerenciaJ.selectedIndex].text;

localStorage.setItem("actividad", JSON.stringify({ 

  nivel: nivelJSON,
  area: areaJSON,
  actividad: actividadJSON,
  gerencia:gerenciaJSON,
  id_nivel: nivelJ.value,
  id_area: areaJ.value,
  id_actividad: actividadJ.value,

 }))
 console.log(actividad)
   window.location = `/resumen`
    });
 </script>


<script>
    const anterior = document.getElementById("anterior")
    anterior.addEventListener("click", async (e) => {
        e.preventDefault()
        window.location = `/`
    });
</script>